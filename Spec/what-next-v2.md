we need to create new shchema for calls where we store all call details

{
    canditateId:"",
    status:"",
    recordingUrl:"",
    agentId:""
}

on agent page
 - we need allow user to enter question or ask AI where they can give prompt we need to call ai and show the ai suggested questions user can edit and update derlte etc 
 - for each pillar we need to have some predefined prompt file when user selected also let the user to edit change with manually or use AI
-  also in the agent allow use to select or create canditate and add them to that agent once user added we need to generate call doc for each candiate with details 
- we need to have 3 tab on agent page config,candiates,calls
    - on canditates show all canditate belong that agent 
    - calls schdeuled for the candiate with details and allow user to select the call and start 


